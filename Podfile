# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BrainMove' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BrainMove
  pod 'MaterialComponents', :inhibit_warnings => true
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  pod 'Firebase/Auth'

  target 'BrainMoveTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BrainMoveUITests' do
    # Pods for testing
  end

end
